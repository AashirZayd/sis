"use server";
export async function actionTarget001(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget002(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget003(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget004(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget005(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget006(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget007(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget008(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget009(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget010(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget011(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget012(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget013(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget014(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget015(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget016(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget017(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget018(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget019(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget020(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget021(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget022(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget023(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget024(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget025(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget026(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget027(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget028(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget029(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget030(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget031(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget032(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget033(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget034(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget035(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget036(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget037(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget038(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget039(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget040(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget041(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget042(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget043(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget044(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget045(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget046(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget047(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget048(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget049(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget050(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget051(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget052(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget053(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget054(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget055(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget056(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget057(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget058(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget059(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget060(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget061(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget062(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget063(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget064(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget065(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget066(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget067(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget068(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget069(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget070(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget071(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget072(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget073(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget074(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget075(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget076(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget077(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget078(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget079(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget080(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget081(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget082(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget083(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget084(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget085(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget086(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget087(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget088(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget089(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget090(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget091(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget092(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget093(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget094(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget095(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget096(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget097(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget098(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget099(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

export async function actionTarget100(val: { id: number; key: string }) {
  if (val.id < 0) throw new RangeError("Negative id");
  return { id: val.id, key: val.key.toLowerCase() };
}

